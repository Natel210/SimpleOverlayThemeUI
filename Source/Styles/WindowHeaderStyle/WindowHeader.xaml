<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:controls="clr-namespace:SimpleOverlayTheme.Controls"
                    xmlns:converters="clr-namespace:SimpleOverlayTheme.Converters"
                    xmlns:helpers="clr-namespace:SimpleOverlayTheme.Helpers"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/SimpleOverlayTheme;component/Styles/BorderStyle/Border.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/SimpleOverlayTheme;component/Styles/BorderStyle/Overlay.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/SimpleOverlayTheme;component/Styles/ButtonStyle/Button.xaml"/>
        <ResourceDictionary Source="pack://application:,,,/SimpleOverlayTheme;component/Styles/ButtonStyle/ButtonMask.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <Style TargetType="{x:Type controls:WindowHeader}">
        <Setter Property="Content" Value="{x:Null}" />
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Label Content="{Binding}"
                           HorizontalContentAlignment="{Binding RelativeSource={RelativeSource AncestorType=controls:WindowHeader}, Path=HorizontalContentAlignment}"
                           VerticalContentAlignment="{Binding RelativeSource={RelativeSource AncestorType=controls:WindowHeader}, Path=VerticalContentAlignment}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="CloseButtonTemplate" >
            <Setter.Value>
                <DataTemplate>
                    <Button Content="X" Style="{StaticResource SimpleOverlayTheme.Styles.ButtonStyle.Button}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="MaximizeRestoreButtonTemplate">
            <Setter.Value>
                <DataTemplate>
                    <ToggleButton Content="O"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="MinimizeButtonTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Button Content="_" Style="{StaticResource SimpleOverlayTheme.Styles.ButtonStyle.Button}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="TailContent" Value="{x:Null}"/>
        <Setter Property="TailContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Label Content="{Binding}"
                           HorizontalContentAlignment="{Binding RelativeSource={RelativeSource AncestorType=controls:WindowHeader}, Path=HorizontalContentAlignment}"
                           VerticalContentAlignment="{Binding RelativeSource={RelativeSource AncestorType=controls:WindowHeader}, Path=VerticalContentAlignment}"/>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="WindowUnlocked" Value="True"/>
        <Setter Property="VisibleClose" Value="Visible"/>
        <Setter Property="VisibleMaximizeRestore" Value="Visible"/>
        <Setter Property="VisibleMinimize" Value="Visible"/>
        <Setter Property="VisibleTail" Value="Visible"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:WindowHeader}">
                    <Border Background="Transparent" BorderBrush="Transparent" BorderThickness="0" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <Border.Clip>
                            <MultiBinding Converter="{StaticResource CornerClipConverter}">
                                <Binding Path="ActualWidth" RelativeSource="{RelativeSource TemplatedParent}"/>
                                <Binding Path="ActualHeight" RelativeSource="{RelativeSource TemplatedParent}"/>
                                <Binding Path="(helpers:CornerRadiusHelper.CornerRadius)" RelativeSource="{RelativeSource TemplatedParent}"/>
                            </MultiBinding>
                        </Border.Clip>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Grid.ColumnSpan="3"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    />

                            <ContentPresenter Grid.Column="0"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            
                            <ContentPresenter Grid.Column="1"
                                              Content="{TemplateBinding TailContent}"
                                              ContentTemplate="{TemplateBinding TailContentTemplate}"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"
                                              Visibility="{TemplateBinding VisibleTail}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            
                            <Grid Grid.Column="2" HorizontalAlignment="Right">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="0" ContentTemplate="{TemplateBinding MinimizeButtonTemplate}" Visibility="{TemplateBinding VisibleMinimize}"/>
                                <ContentPresenter Grid.Column="1" ContentTemplate="{TemplateBinding MaximizeRestoreButtonTemplate}" Visibility="{TemplateBinding VisibleMaximizeRestore}"/>
                                <ContentPresenter Grid.Column="2" ContentTemplate="{TemplateBinding CloseButtonTemplate}" Visibility="{TemplateBinding VisibleClose}"/>
                            </Grid>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>


    </Style>
</ResourceDictionary>