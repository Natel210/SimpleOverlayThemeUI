<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:helpers="clr-namespace:SimpleOverlayTheme.Helpers"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Overlay Default Border -->
    <Style x:Key="SimpleOverlayTheme.Styles.BorderStyle.Overlay" TargetType="Border">
        <!--General-->
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Thickness.Default}"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="IsEnabled" Value="False"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <!--helpers-->
        <Setter Property="helpers:BorderHelper.OverlayState" Value="Default"/>
        <Setter Property="helpers:OverlayDesplayHelper.Background" Value="True"/>
        <Setter Property="helpers:OverlayDesplayHelper.BackgroundAtDefault" Value="True"/>
        <Setter Property="helpers:OverlayDesplayHelper.Outline" Value="True"/>
        <Setter Property="helpers:OverlayDesplayHelper.OutlineAtDefault" Value="True"/>
        <Style.Triggers>
            <!--Background-->
            <Trigger Property="helpers:OverlayDesplayHelper.Background" Value="False">
                <Setter Property="Background" Value="Transparent"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Active"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Background" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Background.Active}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Default"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Background" Value="True"/>
                    <Condition Property="helpers:OverlayDesplayHelper.BackgroundAtDefault" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Background.Default}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Default"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Background" Value="True"/>
                    <Condition Property="helpers:OverlayDesplayHelper.BackgroundAtDefault" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="Transparent"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Disable"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Background" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Background.Disable}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Mouseover"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Background" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Background.Mouseover}"/>
            </MultiTrigger>
            <!--Outline-->
            <Trigger Property="helpers:OverlayDesplayHelper.Outline" Value="False">
                <Setter Property="BorderBrush" Value="Transparent"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Active"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Outline" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Outline.Active}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Default"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Outline" Value="True"/>
                    <Condition Property="helpers:OverlayDesplayHelper.OutlineAtDefault" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Outline.Default}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Default"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Outline" Value="True"/>
                    <Condition Property="helpers:OverlayDesplayHelper.OutlineAtDefault" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="Transparent"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Disable"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Outline" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Outline.Disable}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="helpers:BorderHelper.OverlayState" Value="Mouseover"/>
                    <Condition Property="helpers:OverlayDesplayHelper.Outline" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Outline.Mouseover}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>