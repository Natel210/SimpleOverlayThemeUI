<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:overlayHelper="clr-namespace:SimpleOverlayTheme.Helpers.Overlay"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <!--<ResourceDictionary Source="pack://application:,,,/SimpleOverlayTheme;component/ThemeSystem/LastTheme.xaml"/>-->
    </ResourceDictionary.MergedDictionaries>
    
    <!-- Default Border -->
    <Style x:Key="SimpleOverlayTheme.Border.Default" TargetType="Border" >
        <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.ColorPalette.Background}"/>
        <Setter Property="BorderThickness" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Thickness.Default}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.ColorPalette.Outline}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    
    <!-- Empty Border -->
    <Style x:Key="SimpleOverlayTheme.Border.Empty" TargetType="Border" >
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="{DynamicResource SimpleOverlayTheme.DefaultThickness.Zero}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
    
    <!-- Overlay Default Border -->
    <Style x:Key="SimpleOverlayTheme.Border.Overlay.Default" TargetType="Border">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="{DynamicResource SimpleOverlayTheme.DefaultThickness.Default}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsEnabled" Value="False"/>
        <Setter Property="borderHelper:Overlay.IsDisable" Value="False"/>
        <Setter Property="borderHelper:Overlay.IsDefault" Value="False"/>
        <Setter Property="borderHelper:Overlay.IsMouseOver" Value="False"/>
        <Setter Property="borderHelper:Overlay.IsActive" Value="False"/>
        <Setter Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
        <Setter Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsDisable" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Background.Disable}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsDefault" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Background.Default}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Background.MouseOver}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsActive" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Background.Active}"/>
            </MultiTrigger>

            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsDisable" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.Disable}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsDefault" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.Default}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.MouseOver}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsActive" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.Active}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>
    
    <!-- Overlay Mask Border -->
    <Style x:Key="SimpleOverlayTheme.Border.Overlay.Mask" TargetType="Border">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="{DynamicResource SimpleOverlayTheme.DefaultThickness.Zero}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.Default}"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="IsHitTestVisible" Value="False"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="IsEnabled" Value="False"/>
        <Setter Property="OpacityMask">
            <Setter.Value>
                <ImageBrush ImageSource="pack://application:,,,/SimpleOverlayTheme;component/SolutionFolder/Resource/Mask/empty.png"/>
            </Setter.Value>
        </Setter>
        <Setter Property="borderHelper:Overlay.IsDisable" Value="False"/>
        <Setter Property="borderHelper:Overlay.IsDefault" Value="False"/>
        <Setter Property="borderHelper:Overlay.IsMouseOver" Value="False"/>
        <Setter Property="borderHelper:Overlay.IsActive" Value="False"/>
        <Setter Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
        <Setter Property="borderHelper:Overlay.OutlineVisible" Value="False"/>
        <Style.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsDisable" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Mask.Foreground.Disable}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsDefault" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Mask.Foreground.Default}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.OverlayMask.Foreground.MouseOver}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.BackgroundVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsActive" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Mask.Foreground.Active}"/>
            </MultiTrigger>

            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsDisable" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.Disable}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsDefault" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.Default}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.MouseOver}"/>
            </MultiTrigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="borderHelper:Overlay.OutlineVisible" Value="True"/>
                    <Condition Property="borderHelper:Overlay.IsActive" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="BorderBrush" Value="{DynamicResource SimpleOverlayTheme.ThemeKey.Overlay.Border.Outline.Active}"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>
    